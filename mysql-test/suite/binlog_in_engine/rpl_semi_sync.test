--source include/have_innodb_binlog.inc

# Just to ensure that SHOW MASTER STATUS in the test is stable.
--let $binlog_name= binlog-000001.ibb
--let $binlog_size= 262144
--source include/wait_for_engine_binlog.inc

--source ../rpl/t/rpl_semi_sync.test

# A small trick, we include this again so it can override the .cnf from
# the --source include/master-slave.inc in the included test case.
--source include/have_innodb_binlog.inc
