# cleanup credential manager just in case last run crashed in the middle
CREATE USER 'u' IDENTIFIED BY 'pass';
# Login with correct password to store it
# Check expected key is in the credential manager
# Login without passing password (succeeds)
# Login with -P ("interactively"), does not ask for password
# Login with wrong password (fails)
# Remove entry from credential manager
# Login without passing password (fails, credential is no longer in credential manager)
DROP USER u;
