source include/have_gen_embedding.inc;

--replace_result $OPENAI_PORT OPENAI_PORT

# This test uses the mockup API server, meaning we have a deterministic API response
# For this reason we can test the actual generated vector

eval set generate_embedding_openai_host="http://127.0.0.1:$OPENAI_PORT/success";
select vec_totext(generate_embedding_openai('abc', 'text-embedding-ada-002'));