#
#  /etc/systemd/system/mariadb@bootstrap.service.d/wsrep-new-cluster.conf
#

[Unit]

ConditionPathExists=

[Service]

# Override the main service for a bootstrap start instance
ExecStart=
ExecStart=/usr/sbin/mysqld $OPTIONS --wsrep-new-cluster

